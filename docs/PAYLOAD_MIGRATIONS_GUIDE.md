# Payload CMS Migrations Guide

> **Цей документ є довідкою для роботи з міграціями в Payload CMS.**
> Створено: 2026-02-03 після вирішення критичної помилки з порожніми сторінками в адмінці.
> 
> **Для AI-агентів (Cursor/Claude):** див. також `.cursor/skills/payload-migrations/SKILL.md`

---

## 📋 Зміст

1. [Проблема, яка була](#проблема-яка-була)
2. [Причини помилки](#причини-помилки)
3. [Правильний підхід до міграцій](#правильний-підхід-до-міграцій)
4. [Коли використовувати ручні міграції](#коли-використовувати-ручні-міграції)
5. [Workflow для нових колекцій](#workflow-для-нових-колекцій)
6. [Чеклист перевірки](#чеклист-перевірки)
7. [Діагностика проблем](#діагностика-проблем)

---

## ❌ Проблема, яка була

### Симптоми:
- Сторінки деталей документів в адмінці Payload — **повністю порожні**
- Помилка **"Hydration failed"** в Next.js dev tools
- Список колекцій працює, але редагування будь-якого документа — ні
- Створення нових документів працює, редагування існуючих — ні

### Причина:
**Ручне створення міграцій** без додавання всіх необхідних зв'язків, які Payload CMS очікує для коректної роботи.

---

## 🔍 Причини помилки

### 1. Відсутній зв'язок у `payload_locked_documents_rels`

Payload використовує таблицю `payload_locked_documents_rels` для блокування документів при редагуванні. При ручному створенні нової колекції ми не додали колонку `{collection}_id` до цієї таблиці.

**Що очікує Payload:**
```sql
-- Для кожної колекції потрібна колонка в payload_locked_documents_rels
ALTER TABLE payload_locked_documents_rels 
ADD COLUMN categories_id integer REFERENCES categories(id) ON DELETE CASCADE;
```

**Як виглядає правильна структура:**
```
payload_locked_documents_rels
├── users_id
├── testimonials_id
├── media_id
├── payload_folders_id
└── categories_id  ← Потрібно додати для кожної нової колекції!
```

### 2. Локалізація без відповідних таблиць

Якщо в `payload.config.ts` увімкнена `localization`, Payload очікує таблиці `{collection}_locales` для **всіх** колекцій з полями `localized: true`.

**Проблема:**
```typescript
// В колекції є localized: true
fields: [
  { name: 'name', type: 'text', localized: true }
]

// Але таблиці testimonials_locales не існує!
```

### 3. Застаріла/недійсна конфігурація

Опція `cookies` в `payload.config.ts` не підтримується в Payload 3.x:
```typescript
// ❌ Неправильно - викликає TypeScript помилку
cookies: {
  suffix: 'w2s-payload',
  sameSite: 'lax',
}
```

---

## ✅ Правильний підхід до міграцій

### Основне правило:
> **Завжди використовуй автоматичну генерацію міграцій для змін схеми бази даних.**

### Команди Payload для міграцій:

| Команда | Призначення |
|---------|-------------|
| `npx payload migrate:create` | Створити нову міграцію (автоматично аналізує різницю схем) |
| `npx payload migrate:create --name add-feature` | Створити міграцію з кастомною назвою |
| `npx payload migrate` | Запустити всі pending міграції |
| `npx payload migrate:down` | Відкотити останню міграцію |
| `npx payload migrate:status` | Показати статус міграцій |

### Як працює автогенерація:

```bash
# 1. Ти змінюєш код (додаєш колекцію, поле, тощо)

# 2. Payload порівнює:
#    - Поточну схему в коді (колекції, поля)
#    - Поточну схему в базі даних

# 3. Генерує міграцію з усіма потрібними змінами:
#    - CREATE TABLE
#    - ALTER TABLE (для payload_locked_documents_rels!)
#    - CREATE INDEX
#    - тощо
```

---

## 🔧 Коли використовувати ручні міграції

### ✅ Підходить для ручних міграцій:

| Сценарій | Приклад |
|----------|---------|
| **Seed-дані** | Наповнення початковими категоріями, тегами |
| **Міграція даних** | Перенесення даних з однієї колонки в іншу |
| **Складні трансформації** | Зміна формату даних, об'єднання записів |
| **Очищення** | Видалення застарілих даних |

### ❌ НЕ підходить для ручних міграцій:

| Сценарій | Чому |
|----------|------|
| **Нова колекція** | Потрібні зв'язки до системних таблиць Payload |
| **Нове поле** | Payload може мати власні вимоги до індексів |
| **Локалізація** | Складна структура з окремими таблицями |
| **Зміна типу поля** | Payload використовує специфічні типи |

---

## 📝 Workflow для нових колекцій

### Крок 1: Створити файл колекції

```typescript
// src/collections/Tags.ts
import { CollectionConfig } from 'payload'

export const Tags: CollectionConfig = {
  slug: 'tags',
  labels: {
    singular: 'Тег',
    plural: 'Теги',
  },
  admin: {
    useAsTitle: 'name',
    group: 'Блог',
  },
  access: {
    read: () => true,
  },
  fields: [
    { name: 'name', type: 'text', required: true },
    { name: 'slug', type: 'text', required: true, unique: true },
  ],
}
```

### Крок 2: Додати до payload.config.ts

```typescript
import { Tags } from './src/collections/Tags'

export default buildConfig({
  collections: [
    Users,
    Testimonials,
    Media,
    Categories,
    Tags,  // ← Додати сюди
  ],
  // ...
})
```

### Крок 3: Згенерувати міграцію АВТОМАТИЧНО

```bash
npx payload migrate:create --name add-tags-collection
```

Payload створить міграцію з:
- CREATE TABLE tags
- ALTER TABLE payload_locked_documents_rels ADD COLUMN tags_id
- Всі потрібні індекси

### Крок 4: Запустити міграцію

```bash
npx payload migrate
```

### Крок 5: (Опціонально) Seed-дані

Якщо потрібні початкові дані — створити **окрему ручну міграцію**:

```typescript
// src/migrations/20260203_XXXXXX_seed-tags.ts
export async function up({ db }: MigrateUpArgs): Promise<void> {
  await db.execute(sql`
    INSERT INTO "tags" ("name", "slug") VALUES 
    ('Digital Nomad', 'digital-nomad'),
    ('Віза', 'viza');
  `)
}
```

---

## ✅ Чеклист перевірки

Перед запуском в production, перевір:

- [ ] Міграція створена через `npx payload migrate:create`
- [ ] `payload_locked_documents_rels` має колонку для нової колекції
- [ ] Якщо є `localized: true` поля — увімкнена глобальна локалізація
- [ ] Якщо увімкнена локалізація — є таблиці `{collection}_locales`
- [ ] Немає TypeScript помилок в `payload.config.ts`
- [ ] Після запуску міграції — перезапустити dev server
- [ ] Видалити `.next` кеш якщо є проблеми

---

## 🔍 Діагностика проблем

### Порожня сторінка редагування

1. **Перевірити консоль браузера** — шукати "Hydration" помилки
2. **Перевірити структуру БД:**
   ```bash
   psql -d your_db -c "\d payload_locked_documents_rels"
   ```
3. **Переконатися, що є колонка для колекції**

### Помилка після додавання локалізації

1. **Перевірити чи є таблиці locales:**
   ```bash
   psql -d your_db -c "SELECT table_name FROM information_schema.tables WHERE table_name LIKE '%_locales%';"
   ```
2. **Тимчасове рішення** — закоментувати `localized: true` в полях або `localization` в конфігу

### Помилки TypeScript в payload.config.ts

1. **Перевірити актуальність опцій** — деякі опції змінюються між версіями
2. **Видалити невідомі опції** (як `cookies` в Payload 3.x)

---

## 📚 Корисні посилання

- [Payload CMS Migrations Docs](https://payloadcms.com/docs/database/migrations)
- [Payload CMS Collections](https://payloadcms.com/docs/configuration/collections)
- [Payload CMS Localization](https://payloadcms.com/docs/configuration/localization)

---

## 📅 Історія змін

| Дата | Опис |
|------|------|
| 2026-02-03 | Створено документ після вирішення проблеми з Hydration error |

Тестовий коміт